export class AudioService { master=1;music=1;sfx=1;ambient=1;private context?:AudioContext; private ensure(){this.context??=new AudioContext();return this.context;} tone(kind:'clue'|'transaction'|'restore'){try{const c=this.ensure(),o=c.createOscillator(),g=c.createGain();o.frequency.value=kind==='clue'?740:kind==='transaction'?420:560;g.gain.value=.035;g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.18);o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+.18);}catch{/* audio unavailable */}} pause(){} }
